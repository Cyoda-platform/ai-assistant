Please now call add_controller_file. It should have the same api, but we no longer need any reference of workflow functions, use entity_service without these functions. We have finished their migration